language: ruby
rvm:
  - "3.3.12"
cache: bundler
env:
  # http://docs.travis-ci.com/user/environment-variables/#Global-Variables
  global:
    - BITCOINORG_BUILD_TYPE=deployment

before_install:
  - "gem update --system"
  - "gem install bundler"

script: make travis
